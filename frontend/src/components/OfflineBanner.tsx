"use client";
// Stub component — full implementation lives in the offline-detection feature branch.
// This file exists so layout.tsx compiles while that branch is pending merge.
export default function OfflineBanner() {
  return null;
}
